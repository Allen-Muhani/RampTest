import { WidgetEventTypes } from './types';
export function initEventListenersDict() {
  const widgetEventTypes = Array.from(Object.values(WidgetEventTypes));
  return [...widgetEventTypes].reduce((listenersDict, eventType) => {
    listenersDict[eventType] = [];
    return listenersDict;
  }, {});
}
export function getRandomIntString() {
  return String(Math.floor(Math.random() * 10000000));
}
//# sourceMappingURL=utils.js.map