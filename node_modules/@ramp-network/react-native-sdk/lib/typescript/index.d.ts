import { IHostConfig, TAllEvents } from './types';
export { WidgetEventTypes as WidgetEventTypes } from './types';
export default class RampSdk {
    private config;
    private _listeners;
    private _instanceId;
    constructor(config: IHostConfig);
    show(): RampSdk;
    on<T extends TAllEvents>(type: T['type'] | '*', callback: (event: T) => any): RampSdk;
    unsubscribe(type: TAllEvents['type'] | '*', callback: (event: TAllEvents) => any): RampSdk;
    private _subscribeToRampEvents;
    private _dispatchEvent;
}
